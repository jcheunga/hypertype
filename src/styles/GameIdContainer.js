import styled from 'styled-components/native';

const GameIdContainer = styled.View`
  flex-direction: row;
  align-items: center;
  justify-content: flex-end;
  margin-bottom: 15px;
`;

export default GameIdContainer;