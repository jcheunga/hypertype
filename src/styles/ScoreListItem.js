import styled from 'styled-components/native';

const ScoreListItem = styled.View`
  flex-direction: row;
  justify-content: space-between;
  border-bottom-width: 1;
  border-bottom-color: #dbdbdb;
  margin-bottom: 10px;
  padding-bottom: 10px;
`;

export default ScoreListItem;